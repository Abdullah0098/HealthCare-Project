version: 2

sources:
  - name: src_healthcare_raw
    database: healthcare  
    schema: STAR_SCHEMA  
    tables:
      - name: DIM_CPTCODE
      - name: DIM_DATE
      - name: DIM_DIAGNOSIS
      - name: DIM_LOCATION
      - name: DIM_PATIENT
      - name: DIM_PAYER
      - name: DIM_PHYSICIAN
      - name: DIM_TRANSACTION
      - name: DIM_DATADICTIONARY
